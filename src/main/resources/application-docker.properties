properties
# application-docker.properties
# Only override what is different for Docker
# Docker MySQL container name as hostname
spring.datasource.url=jdbc:mysql://mysql-db:3306/springboot
spring.datasource.username=root
spring.datasource.password=root
# Show SQL in docker logs too
spring.jpa.show-sql=true